# DISABLED: This project uses GitHub Pages for static hosting.
# Do NOT rename this file back - it causes Freebuff to deploy as Azure Function.

name: Legacy Azure Functions (DISABLED - Do Not Enable)

on:
  # Never runs automatically - only manual with safety word
  workflow_dispatch:

# This job does nothing - it exists only to prevent Azure Function deployment
jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled"
